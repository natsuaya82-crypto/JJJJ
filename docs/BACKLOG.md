# 気になった点の置き場（リファクタリング中は手を付けない）

**いまやっているのは構造の整理だけです。中身（仕様・バランス・不具合の修理）は後回しにします。**
作業中に「これはおかしいのでは」と気づいたものを、忘れないようにここへ貯めます。

## 使い方

- **見つけたらここに足す。その場で直さない。**
  直すかどうか・どう直すかを決めるのはオーナーです（CLAUDE.md「勝手に決めないこと」）
- 1件につき「何が起きるか」「どこ」「いつ・どうやって見つけたか」を書く
- 推測で「将来困るかも」は書かない。**いま実際にそうなっているものだけ**
- 片付いたら行を消さずに `済` と結末を書き足す（同じ議論を2回しないため）

---

## A. 仕様・バランス（オーナーが決めること）

### A-1. 移籍金が誕生日をまたいだ瞬間に落ちる

年齢倍率が段になっているので、**能力が1も変わっていないのに**誕生日で移籍金が下がります。

| 誕生日 | 倍率 | 下落 |
|---|---|---|
| 22→23歳 | 5→4 | −20% |
| 27→28歳 | 4→3 | −25% |
| 31→32歳 | 3→2 | −33% |

- どこ … `src/utils/playerUtils.ts` の `transferFeeAgeMultiplier`
- 現状 … CLAUDE.md に現行仕様として書いてあるとおりの動き。**壊れてはいない**
- 気になる点 … 「27歳の主力は28歳になる前に売る」が最適解になる。
  遊び方として意図どおりか
- 見つけ方 … 2026-08-11、点検の書き換え中に実測（数値は統括が再測して確認）

### A-2. 契約の年俸ダイヤルの上限

いまの上限は **交渉8,000万／ドラフト6,000万**（`src/data/economy.ts` の
`NEGOTIATION_SALARY_MAX` / `DRAFT_SALARY_MAX`）。
オーナーから「上限って1億くらいなかったっけ？」という発言があったが、実装は8,000万。
上げるかどうかは未決。

### A-3. 移籍金の入札で「競り負け」の起き方が変わっている

旧仕様「上回るクラブがいれば即 rejected」→ 現仕様「1回目は countered で上乗せの機会を出す」。
点検（`scripts/check-transfer-bid.ts`）は旧仕様のまま止めてある（B-1）。
**どちらが正なのかを決める必要がある。**

### A-4. 海外クラブがらみの移籍で、お金が湧く／消える

CLAUDE.md は「海外クラブの資金も本物。買えば減り、売れば増える」と書いているが、
**それが守られているのはオフシーズンの日本↔海外だけ**だった。

| 経路 | いつ | 海外クラブの資金 |
|---|---|---|
| 日本↔海外（`simulateCrossBorderTransfers`） | オフ | ✅ 動く |
| 海外↔海外（`simulateForeignTransferMarket`） | オフ | ❌ **そもそも移籍金を取っていない**（0円で引き抜ける） |
| 自チーム↔海外（買い取り打診の承諾など） | シーズン中 | ❌ **動かない**（自チーム側だけ増減する） |

- どこ … `src/engine/foreignTransfers.ts` ／ `src/store/marketOps.ts` の `sellMove` 経路
- 影響 … 海外クラブはシーズン中いくら買っても資金が減らない。
  CLAUDE.md が「一度直した」と書いている状態が、シーズン中には残っている
- **今回のリファクタリングが原因ではない**（分解の前から同じ形）
- 詳細 … `docs/AUDIT_MOVEPLAYER.md` の 2-1・2-2
- 直すときの注意 … `movePlayer` の中で海外を扱わないこと（`teams` しか知らない設計を崩さない）。
  呼び出し元かその1つ外側で精算する。**先に golden を足すこと**（C-1）

### A-5. 大陸予選の代表20人が、選手の削除から守られていない

`worldAthleticsResults[].continentals[].squads`（欧州・アフリカ・アメリカの大陸予選の
代表20人×3地域）が `savePruning` の `protectedIds` に入っていない。
**代表の記録がここにしか無い**（`worldRepresentatives` へ二重保存していない）ので、
他の条件で守られていない選手が消えると「大陸代表だった」という事実と
駅伝代表バッジ（`utils/badges.ts`）が静かに消える。

- クラッシュはしない。画面（`NationalResultPage`）は通過国名だけを見せていて
  20人の名簿は出さないので、「20人のはずが18人」にもならない
- 本戦とアジア予選の代表（`worldTournament.squads` ／ `worldAthleticsResults[].squads`）は
  守られている。**大陸予選だけが漏れている**
- 直すなら `src/engine/savePruning.ts` のループに1行足すだけ
- 決めること … 「消えてよい割り切り」か「本戦と同じ扱いに揃える」か
- 詳細 … `docs/AUDIT_SAVEPRUNING.md`（2026-08-11 の逆方向監査）

### A-6. 「譲ります」の決着で本人が断ったとき、止まるのは1クラブだけ

`settleSaleAnswers`（`src/store/marketOps.ts`）に、本人が断ったら**そのとき打診していた
全クラブ**を今季止めるための処理があります。コメントにも「ここを入れ忘れていたので、
『移籍しないことに決めました』の直後に同じクラブからまた打診が並んでいた」と書いてあります。

**この処理は一度も走っていません。** 条件が `outcome === 'refused'` なのに対して、
呼んでいる `acceptIncomingOffer` が返すのは `invalid` / `roster_min` / `pending` /
`refused_by_player` / `sold` の5つだけで、`refused` は**逆提示でしか起きません**
（`src/utils/offerResult.ts` にもそう書いてあります）。

実際に起きていること … 止まるのは `acceptIncomingOffer` の中で控える**GMが選んだ1クラブ**だけ。
同じ選手に打診していた他のクラブは今季また来られます。

- どこ … `src/store/marketOps.ts` の `settleSaleAnswers`（`refusedClubs` の枝）
- 決めること … 「本人が断った相手だけ止める」（いまの実物）か
  「話が乗っていた全クラブを止める」（コメントの意図）か。
  **どちらでも辻褄は合う**ので、遊び方として選ぶ話です
- **今回のリファクタリングが原因ではありません**（分解の前から同じ形）
- 見つけ方 … 2026-08-11、買い取り打診の関門を `engine/saleOfferGate` へ切り出す作業中に、
  `saleRefused` を書いている場所を数えて発見（3箇所のうち1つが到達不能）

### A-7. CPU間トレードが1件も成立しない

オフシーズンの「CPU間トレード」（`beginSeasonDraft` の⑤）が、**条件が噛み合わず一度も
成立していません**。組み合わせを数えたところ（2030年・国内51クラブ・1オフ）

| 段階 | 通った数 |
|---|---|
| 買い手として検討されたクラブ | 51 → **12**（39クラブは在籍23人以上で門前払い） |
| 出す選手（自分のところで出番が無い）が見つかった | 12（全部見つかっている） |
| 相手の選手との組み合わせ | 11,753通り |
| └ もらう側で走れる（`wouldMakeLineup`） | 686 |
| └ 出す側で出番が無い（`hasNoPlayingTime`） | 5,413 |
| └ **両方満たす** | **13** |
| └ さらに釣り合う（`tradeBalance`） | **1** |
| └ さらに売り手も相手を欲しがる | **0** |
| 成立 | **0件** |

**これは引きの悪さではなく、条件が論理的に噛み合っていません。**

    もらう選手は買い手で上位7人   ＝ 買い手で自分より上は6人以下
    出す選手は買い手で15番手以降  ＝ 買い手で自分より上が14人以上
      → 出す選手 ＜ もらう選手
    売り手側でまったく同じ関係を見ると
      → 出す選手 ＞ もらう選手

OVRの大小だけで序列が決まる以上、この2つは同時に立ちません。
成立するのは**OVRが並んだとき**だけで、それは「並び順の何番目か（`i + 1`）」と
「自分より強い人が何人いるか（`squadRankOf`）」という2つの別々の数え方を
混ぜて使っているから開く抜け道です。実際、全員同じOVRの世界を作れば成立します
（`scripts/check-cpu-trade.ts` で実演しています）。

- どこ … `src/engine/cpuOffseason.ts` の `runCpuTrades`（旧 `draftSlice` の⑤）
- 決めること … 条件を緩めて実際に動かすか、この経路は無いものとして畳むか
- **今回のリファクタリングが原因ではありません**（分解の前から同じ形）
- 見つけ方 … 2026-08-11、`beginSeasonDraft` を分解する前に
  「golden がどの枝を通っているか」を枝ごとに壊して確かめたところ、
  ここだけ**壊しても差分が出なかった**（＝一度も通っていない）ので数えた

### A-8. GMの評判の下限が2つある（0 と 1）

同じ `gmRep` を動かす場所が2つあり、**下限が食い違っています**。

| どこ | いつ動くか | 下限 |
|---|---|---|
| `resolveEvent`（イベントの決着） | シーズン中。理事会・スポンサー・予算のイベント | **0** |
| `engine/seasonObjectives.ts` | シーズン終了時の目標達成率 | **1** |

0まで落ちる経路があるので、「評判0」と「評判1」が両方あり得ます。
画面がどちらを底として見せるつもりなのかが決まっていません。

- 決めること … 底を 0 に揃えるか 1 に揃えるか
- **今回のリファクタリングが原因ではありません**（1本化する前から同じ形）
- いまの扱い … 上下限は `utils/condition.ts` の `withGmRep`（0〜100）1本にまとめ、
  `seasonObjectives` 側だけ `Math.max(1, withGmRep(...))` と**1行で見えるように**
  残してあります。挙動はどちらも変えていません
- 見つけ方 … 2026-08-11、`resolveEvent` を表に組み替える作業中に、
  11か所の手書きを数えて発見

---

## B. 止めたままの点検

### B-1. `transfer-bid` … 8件（`pending: 8` で登録済み。これ以上増えたら落ちる）

`済`（2026-08-11）：12件を仕分けて4件を片付けた。分解で engine へ移ったぶん
（`resolveBid` ／ `offerExpiry` ／ 1年ロック ／ `movePlayer`）は `logicSource` へ切り替え。

**残り8件は全部 A-3（入札の勝敗ルール）1つに起因する。** 旧「上回るクラブがいれば
即 rejected」を前提にした assert が連鎖的に落ちている形なので、
**A-3 が決まれば8件まとめて片付く。**

### B-2. `continental` … 分布のゆらぎ（`~` 印）

大陸予選で「上位の通過率 ≦ 下位の通過率」になる世界が **40回に2回**ほど生成される。
テストの不備ではなく、生成された名簿しだいで国同士の強さの差が潰れる回がある。
判定は緩めていない。

**2026-08-11：3回連続で落ちる回を1度観測しました**（`flaky: 3` なので本物の NG として
止まった。直後に走らせ直したら2回目で通った）。40回に2回なら3連続は8,000回に1回の
はずなので、**実際の失敗率が記録より高いか、引き直しが独立していないか**のどちらかです。
どちらなのかは測っていません。頻繁に止まるようなら、まず回数を数えること。

---

## C. 安全網の穴（構造の作業に影響する）

### C-1. golden 検査が届いていない範囲

`済`（2026-08-11）：**穴は塞ぎました。** golden は12本になり、
`store/slices` の大きなアクションはすべて網の中です。

| ファイル | アクション数 | golden |
|---|---|---|
| `store/slices/marketSlice.ts` | 45 | あり（契約更新・買い取り・トレード・獲得オファー） |
| `store/slices/draftSlice.ts` | 13 | あり（`draft-flow` ほか2本） |
| `store/slices/raceSlice.ts` | 8 | あり（`runRace` / `runRace-final` / `race-event` / `race-timetrial`） |

最後に足した2本のねらい：

- `race-event` … `resolveEvent`（178行）は「イベントの種類 × 選んだ肢」の巨大な
  if-else。実際に出るイベントは確率で選ばれるので、**通る枝が引き次第**になる。
  札を自分で並べて **19種 × 3肢＝57件を必ず全部通す**
- `race-timetrial` … `simulateIndividualEvent`（194行）。国内だけの回と海外も出る回の
  2本を走らせる（対象の絞り込みが違う）。**自チームの選手を最強にしてある**——
  そうしないと5,800人中の順位が100位より下になり、カード報酬も世界記録も
  1行も通らない（最初に書いた版が実際にそうだった）

網を作るときの注意は `docs/REFACTORING_DESIGN.md` 12-3。**枝を通っていない網は、緑でも
何も守っていない。** トレードの網は最初に書いた版が「相手が手放すものに見合わない」で
全部はじかれ、成立側を1行も通っていなかった（総当たりで組み合わせを数えて選び直した）。

**`draft-flow` に1箇所だけ穴があります（2026-08-11 に枝ごとに壊して確認）。**

| `beginSeasonDraft` の枝 | 壊すと golden が落ちるか |
|---|---|
| ①CPUの解雇 | ✅ 落ちる |
| ②CPU間移籍 | ✅ 落ちる |
| ③**CPU間トレード** | ❌ **落ちない** → `check-cpu-trade` で塞いだ |
| ④CPU間レンタル | ✅ 落ちる |
| ⑤FA補強 | ✅ 落ちる |

③は**そもそも1件も成立しない**（A-7）ので、成立したあとの処理が網の外にありました。
評価の側（どの組み合わせを見るか）は 11,753 通り通っているので、判定を触れば落ちます。

`済`（2026-08-11）：`engine/cpuOffseason.ts` へ切り出し、成立する世界を手で組んだ
`scripts/check-cpu-trade.ts` を足した（`npm run check` に登録済み・9件）。
移籍記録の種別・交換の向き・1オフ1件の上限・`excludeIds` の書き足しを、
わざと壊して落ちることを確認済み。

### C-2. `load-v39` は実機のセーブが要る

リポジトリに入れられないので、既定では「見送り」。
**セーブ形式（persist の version）を上げるときは必ず手で走らせること。**

---

## D. 実機でしか確かめられないもの

**「Web で緑」と「iOS 実機で確認済み」は別物として扱う。** ここは私たちでは閉じられない。

- `position: fixed`（`<main>` の中に書くと実機だけで下タブに食われる。掃除は進行中）
- viewport ／ キーボード ／ セーフエリア
- Capacitor（Filesystem・AdMob・lifecycle・background/foreground・セーブの書き出し）

変更した範囲がこのどれかに触れたときは、**オーナーの TestFlight 確認をもって完了**とする。

---

## E. 既知の不具合（中身の修理なので後回し）

### E-1. アジア予選が開催されないセーブがある

直す場所は `src/store/bootRepair.ts`（読み込み時の自動修復）。
構造の整理が一段落してから着手する。

---

## F. 構造の宿題（リファクタリングの範囲内）

- **F-1. `movePlayer` の責務の線引き** … `済`（2026-08-11・`docs/AUDIT_MOVEPLAYER.md`）。
  36箇所の呼び出しを全部確認。`teams: []` の6箇所はすべて意図どおりで、
  **「必要な処理が抜けている」呼び出しは無し**。分割の必要も無し。
  ただし海外がらみのお金で2件見つかった → A-4
- **F-2. `savePruning` の逆方向の監査** … `済`（2026-08-11・`docs/AUDIT_SAVEPRUNING.md`）。
  選手IDを持つフィールドを型定義から全部拾って突き合わせ。**壊れる穴は無し**。
  仕様確認が1件だけ出た → A-5
- **F-3. `check-layers` の範囲** … `済`（2026-08-11）。ルール2・3を `src/store` 以下
  ぜんぶへ。違反0件、広げる前後で全49本の ok/NG も差分なし
